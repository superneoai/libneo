[package]
name = "gpui_shared_string"
description = "A permissive shared-string compatibility crate for GPUI"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
schemars = "1"
serde = { version = "1", features = ["derive"] }
