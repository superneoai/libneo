[workspace]
members = [
  "crates/libneo",
  "crates/libneo-gpui",
  "tests/workspace-checks",
  "vendor/ztracing",
]
exclude = [
  "vendor/block",
  "vendor/gpui-shared-string",
  "vendor/gpui-util",
]
resolver = "3"

[workspace.package]
version = "0.1.0-alpha.1"
edition = "2024"
rust-version = "1.97"
authors = ["ACTUAL LTD."]
repository = "https://github.com/superneoai/libneo"
license = "MIT OR Apache-2.0"
publish = false

# Replace GPUI helpers with permissive compatibility crates.
[patch."https://github.com/zed-industries/zed"]
gpui_shared_string = { path = "vendor/gpui-shared-string" }
gpui_util = { path = "vendor/gpui-util" }
ztracing = { path = "vendor/ztracing" }

# Use the crates.io font-kit package required by the pinned Zed revision.
[patch."https://github.com/zed-industries/font-kit"]
zed-font-kit = { version = "=0.14.1-zed" }

# Fix block 0.1.6's uninhabited FFI static type.
[patch.crates-io]
block = { path = "vendor/block" }
